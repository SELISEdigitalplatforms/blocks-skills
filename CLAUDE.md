# Agent instructions

**Read [AGENTS.md](./AGENTS.md). It is the only source of instructions for this repo.**

This file is a pointer and nothing more. It deliberately contains no guidance of its own.

- Read `AGENTS.md` at the start of work here and follow it as written.
- Never duplicate any part of `AGENTS.md` into this file — a second copy will drift and one of them will be wrong.
- Never add guidance here that is not in `AGENTS.md`. If something needs to be recorded, it belongs in `AGENTS.md`; put it there instead.
- If the two ever disagree, `AGENTS.md` wins.
